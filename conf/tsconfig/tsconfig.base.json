{
  "compilerOptions": {
    "module": "esnext", // specifies the module type
    "moduleResolution": "node", // specifies how modules are resolved
    "lib": ["esnext"], // specifies the library files to be included
    "types": ["jest"],
    "target": "es6", // specifies the ECMAScript target version
    "strict": true, // enables strict type-checking
    "noImplicitReturns": true, // disallows implicit return values for functions
    "noUnusedLocals": true, // disallows unused local variables
    "noUnusedParameters": true, // disallows unused function parameters
    "esModuleInterop": true, // enables interoperability between CommonJS and ES modules
    "skipLibCheck": true, // skips type-checking of all declaration files
    "noUncheckedIndexedAccess": true, // disallows unchecked indexing for arrays and objects
    "noEmit": true // prevents the compiler from emitting output files,

  },
  "exclude": ["graphqlTypes.ts", "graphqlOperations.ts"]
}
