[build]
builder = "nixpacks"
buildCommand = "pnpm run nx run @full-stack-typescript-monorepo-starter-with-authentication/server:build"

[deploy]
startCommand = "pnpm run start --prefix server"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
